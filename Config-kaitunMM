local function createProxy()
    local proxy = {}
    local meta = {
        __index = function(t, k)
            return createProxy()
        end,
        __call = function(t, ...)
            return true
        end
    }
    return setmetatable(proxy, meta)
end

return createProxy()